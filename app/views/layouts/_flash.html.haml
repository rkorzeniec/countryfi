- flash.each do |message_type, message|
  .alert{ id: "flash-#{message_type}", class: "#{flash_message_css_class(message_type)} text-md", data: { controller: 'timeout', target: 'timeout.element' } }
    %button{ type: 'button', class: 'close', 'data-dismiss': 'alert' }×
    = message
