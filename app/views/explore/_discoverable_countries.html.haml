.d-flex.flex-row.justify-content-center
  .col-md-12
    .text-md Countries you still haven't explored...
    #countriesAccordion.country-categories-nav.accordion
      %ul.country-categories
        - explore_facade.country_categories.each do |category|
          %li.country-category
            = link_to category, '#', id: "#{category}", data: { toggle: 'collapse', target: "#country-#{category}" }, aria: { controls: "country-#{category}", expanded: "#{all_countries_category(category)}" }, role: 'button'
      - explore_facade.country_categories.each do |category|
        .row{ id: "country-#{category}", class: "country-content collapse #{'show' if all_countries_category(category)}", data: { parent: "#countriesAccordion" }, aria: { labelledby: "#{category}" } }
          - explore_facade.discoverable_countries_by_category(category).each do |country|
            .col-md-4.col-xs-6.col-sm-6.country
              = link_to country.name_common, country_path(country.cca2)
