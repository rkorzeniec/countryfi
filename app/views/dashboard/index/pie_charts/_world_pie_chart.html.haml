- visited_percent = (visited_countries_count / countries_count.to_f) * 100
.panel.box.text-center
  .box-row
    .box-cell.p-t-1.font-size-12.font-weight-semibold World
  .box-row
    .box-cell.p-y-2
      #world_pie_chart.easy-pie-chart{ 'data-percent': "#{visited_percent}" }
        %span= link_to "#{visited_countries_count}/#{countries_count}", checkins_worlds_path
:javascript
  var element = document.querySelector('#world_pie_chart');
  new EasyPieChart(element, {
    animate: 2000,
    scaleColor: false,
    lineWidth: 6,
    lineCap: 'square',
    size: 90,
    trackColor: '#e5e5e5',
    barColor: '#37BC9B'
  });
