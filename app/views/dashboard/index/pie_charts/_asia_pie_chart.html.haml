- total_countries = CountriesCounter.asian_countries_count
- visited_percent = (visited_countries_count / total_countries.to_f) * 100
.stat-panel.text-center
  .stat-row
    .stat-cell.padding-sm.text-xs.text-semibold Asia
  .stat-row
    .stat-cell.padding-sm
      .pie-chart#asia_pie_chart{ 'data-percent': "#{visited_percent}" }
        .pie-chart-label
          = "#{visited_countries_count}/#{total_countries}"
:javascript
  window.PixelAdmin.init.push(function () {
    var pieChartDefaults = {
      animate: 2000,
      scaleColor: false,
      lineWidth: 6,
      lineCap: 'square',
      size: 90,
      trackColor: '#e5e5e5'
    };

    $('#asia_pie_chart').easyPieChart($.extend({}, pieChartDefaults, {
      barColor: '#8CC152'
    }));
  });
