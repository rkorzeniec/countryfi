- visited_percent = (visited_countries_count / countries_count.to_f) * 100
.card
  .card-body.text-center
    .text-sm.text-semibold Europe
    .py-2
      #europe_pie_chart.pie-chart{ 'data-percent': "#{visited_percent}" }
        %span.pie-chart-label= link_to "#{visited_countries_count}/#{countries_count}", checkins_europeans_path
= content_for :js do
  :javascript
    var element = document.querySelector('#europe_pie_chart');
    new EasyPieChart(element, {
      animate: 2000,
      scaleColor: false,
      lineWidth: 6,
      lineCap: 'square',
      size: 90,
      trackColor: '#e5e5e5',
      barColor: '#4A89DC'
    });
