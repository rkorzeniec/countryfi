.d-flex.flex-row.justify-content-center
  %h2.text-center.text-lg Change your password
.d-flex.flex-row.justify-content-center.mt-3.mb-3
  = form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :put }) do |f|
    = devise_error_messages!
    = f.hidden_field :reset_password_token
    .form-group.signin-password
      = f.password_field :password, autofocus: true, autocomplete: 'off', placeholder: :password, class: 'form-control'
    .form-group.signin-password
      = f.password_field :password_confirmation, autocomplete: 'off', placeholder: :password_confirmation, class: 'form-control'
    .form-group
      = f.submit 'Change my password', data: { "disable-with": "Changing..." }, class: 'btn btn-primary btn-block'
      = link_to 'Cancel', new_session_path(resource_name), class: 'btn btn-secondary btn-block'
